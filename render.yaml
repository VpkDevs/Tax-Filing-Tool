services:
  - type: web
    name: tax-filing-tool
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn src.main_app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
    autoDeploy: true
